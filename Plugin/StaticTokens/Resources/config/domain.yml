services:

    Apisearch\Plugin\StaticTokens\Domain\Token\StaticTokenLocator:
        arguments:
            - "%apisearch_plugin.static_tokens.tokens%"
        tags:
            - "apisearch_server.token_locator"
            - "apisearch_server.token_provider"
